apply plugin: 'com.android.library'
apply plugin: 'com.github.dcendents.android-maven'

group='com.github.iamporus'

android {
    compileSdkVersion 29
    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 29
        versionCode 2
        versionName "2.0.200627"
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.1.0'
}
